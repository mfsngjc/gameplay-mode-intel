# 玩法资料采集

采集范围包括新模式、限时玩法，以及原有模式中的基础局内体验更新。用户已于 2026-09-16 明确要求持续整理后者，包括地图机制物、道具、POI 改造和常规体验优化。

- 采集前阅读 `docs/core-experience-updates.md`，按其中的分类、来源、版本与去重规则执行。
- `data/core-updates.json` 是基础体验更新的独立资料源，已接入案例库、动态和时间轴。不要把这些条目计为新增模式，也不要为了展示而塞入 `data/modes.json`。
- 扫描完整官方补丁中的地图、BR/团竞、装备、资源、复活、操作优化等章节；旧审计中的 `system-only` 不能再整体排除，需按是否影响局内行为重新评估。
- 分别维护模式覆盖和基础体验更新覆盖。来源页曾经检查过，不代表其中的常规更新已收齐。基础更新本轮覆盖记录在 `data/core-update-source-coverage.json`。
- 公告日期、实际生效日期、活动期限分别记录。没有写限时不等于永久；预告不能记为已上线。事实与设计推断分开。
- 中文说明使用可核实译名或明确的说明性译法。公开封面继续遵守 README 的官方图片要求。
- 只有 `publicationStatus: ready` 的记录进入页面；新增记录须补官方图、图片来源和日期依据，并通过 `node scripts/validate_core_updates.cjs`。本地修改不自动发布。

## FF 双线与版本详录

- 常规采集必须读取 `/Users/Admin/.codex/skills/br-mode-intel-sync/references/free-fire-patch-dossiers.md`，逐项扫描全篇局内内容和 HTML 表格、删除线。
- Free Fire 用 `ffPlaylists` 显式区分 BR、CS 与其他独立玩法；CS 不等于 Bomb Squad，不修改全站 taxonomy 枚举来混用。卡片、详情、筛选、导出与时间轴保持一致。
- `data/free-fire-patches.json` 保存版本详录；BR/CS 分线，通用章节单列。版本组织卡不计为新模式。跨线记录统计按 ID 去重。
- 已完成详录的七篇基线为 1213、1266、1306、1473、1511、1595、1712。2026-09-19 用户已授权独立 GPT-5.6 Terra 按 SOP 扩展整理 FF 所有可核实的历史基础体验更新，逐批复核入库；目录盘点不等于内容整理完成，局外目录摘要不可称为整篇逐项全译。
- 官网正文图片全部归档并直接展示：相关图通过 `sections[].imageIds` 嵌入小节，总览及其他章节图片直接可见，不得只放折叠图库。保留原图比例、来源和图片内日期证据；BR/CS 筛选时仍可在其余配图区查看本篇全部原图。
- 用 `python3 scripts/build_ff_patch_docs.py` 更新可下载详录，运行 `node scripts/validate_ff_patches.cjs` 检查双线、表格、图片和文档。
- 保持平实中立，不使用口号。说明具体变化、原值→新值、模式/地图/队伍规模、单位、限制、生效时间和原文小节。
- 历史目录不得仅凭 PATCH/UPDATE 标题筛选；按正文处置每篇，发现漏分后重新打开该年份的覆盖状态。画质、性能、HUD 和输入配置按实际对局作用判断，不因设置入口在大厅而整体排除。
