# 地图互动集成 Design QA

final result: passed

核查日期：2026-09-06。源视觉目标使用腾讯国服官方地图工具桌面页与移动页；实现使用本地 `assets/maps/delta-interactive/` 快照和站内 `#maps` 入口。桌面源与实现以 1280 × 720 视口在同一次截图输入中对照；移动源与实现以 390 × 844 视口在同一次截图输入中对照。移动实现在底图加载完成后复核，8 个本地切片均成功加载。

## 结果

- 信息架构：地图页仍叫“地图”，三角洲行动作为游戏卡片；封面点击打开站内互动地图，返回按钮回到地图资料页。
- 布局与节奏：互动地图弹层占满可用视口，顶部本地返回栏固定 54px，桌面左侧官方控制栏和移动端底部“筛选标点”保留。没有出现滚动条遮住地图控制的情况。
- 字体与文字：保留官方地图工具的中文字体回退和点位名称；本地返回栏、快照日期、官方来源使用研究库的可读小号文字。
- 颜色：地图主体、左侧控制栏、点位激活色与官方源保持一致；本地返回栏使用低饱和深绿，能够与地图区分但不抢占地图焦点。
- 图像：20 张地图、55 套地图层和 11,859 个本地切片已复制到站内；官方最高层切片原样保留，低层由本地缩略生成。缺失的官方图标以点位文字兜底，不伪造图标。
- 文案：地图页明确写出“本地互动地图”；保留官方来源链接和“底图 © 腾讯”标识。

## 交互回归

- 6 张烽火地带地图：全选后点位数量可见，零号大坝验证了“保险箱”筛选、搜索结果、快速定位、B1／1F／2F 楼层切换。
- 14 张全面战场地图：逐张加载无脚本错误；攀升验证了区域 A、区域 B、PC／移动、攻防／占领切换，切换后请求了 `map_pc_zl` 等对应本地层。
- 移动端：390 × 844 验证搜索、点位筛选与“查看标点”，与官方移动源的地图主体、顶部选择器和底部筛选操作保持同一结构。
- 键盘：本地适配器为地图、楼层、点位筛选和战场区域补充可聚焦及 Enter／Space 激活。
- 离线资源：运行时地图切片、图标、字体和数据不再请求腾讯 CDN；仅保留用户点击后打开的官方来源链接。地图页入口、静态底图下载和互动弹层均通过本地服务器验证。

## 已知边界

官方源的 24 张潮汐监狱边界切片返回 404，已记录在 `local-only/delta-tile-errors.json` 并使用空白切片避免破图；不补画地图内容。少数官方点位图标源本身不存在，本地显示点位名称并保留可点击详情。分享、统计、反馈和游戏内桥接 SDK 已从本地版本移除。

## 复核命令

```bash
node --check maps.js
node --check workspace.js
node --check app.js
node scripts/validate_taxonomy.cjs
python3 scripts/build_public_site.py
git diff --check
```
